from setuptools import setup, find_packages
setup(name='libopenttd',
      version='0.1.0',
      description='Python implementation of OpenTTD\'s network protocols',
      license='Creative Commons',
      packages=find_packages(),
      install_requires=[
      ],
      data_files=[],
      zip_safe=False)
